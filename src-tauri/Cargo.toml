[package]
name = "study-cycle-timer"
version = "0.1.0"
description = "Minimal desktop timer for the 20-3-40-2-10-20 study cycle"
authors = ["Codex"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
